def add_numbers(a: float, b: float) -> float:
    """Return the sum of two numbers.

    This helper is intentionally small and typed so it can be unit tested
    independently from the FastAPI application.
    """
    return a + b
